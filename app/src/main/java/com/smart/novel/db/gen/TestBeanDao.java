package com.smart.novel.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.smart.novel.bean.TestBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TEST_BEAN".
*/
public class TestBeanDao extends AbstractDao<TestBean, String> {

    public static final String TABLENAME = "TEST_BEAN";

    /**
     * Properties of entity TestBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Msg = new Property(0, String.class, "msg", true, "MSG");
        public final static Property Success = new Property(1, boolean.class, "success", false, "SUCCESS");
    }


    public TestBeanDao(DaoConfig config) {
        super(config);
    }
    
    public TestBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TEST_BEAN\" (" + //
                "\"MSG\" TEXT PRIMARY KEY NOT NULL ," + // 0: msg
                "\"SUCCESS\" INTEGER NOT NULL );"); // 1: success
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TEST_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TestBean entity) {
        stmt.clearBindings();
 
        String msg = entity.getMsg();
        if (msg != null) {
            stmt.bindString(1, msg);
        }
        stmt.bindLong(2, entity.getSuccess() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TestBean entity) {
        stmt.clearBindings();
 
        String msg = entity.getMsg();
        if (msg != null) {
            stmt.bindString(1, msg);
        }
        stmt.bindLong(2, entity.getSuccess() ? 1L: 0L);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public TestBean readEntity(Cursor cursor, int offset) {
        TestBean entity = new TestBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // msg
            cursor.getShort(offset + 1) != 0 // success
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TestBean entity, int offset) {
        entity.setMsg(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setSuccess(cursor.getShort(offset + 1) != 0);
     }
    
    @Override
    protected final String updateKeyAfterInsert(TestBean entity, long rowId) {
        return entity.getMsg();
    }
    
    @Override
    public String getKey(TestBean entity) {
        if(entity != null) {
            return entity.getMsg();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TestBean entity) {
        return entity.getMsg() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
